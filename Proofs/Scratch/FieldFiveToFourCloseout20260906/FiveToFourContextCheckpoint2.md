# SF-F-CANON-001 checkpoint

Promoted in `Proofs/BilinearComplexity/FieldFiveToFourContext.lean`: source/source outer source/target formulas; signed residual source singleton; residual source canonical equality; residual target original-target formula; occupied source/source residual-context formula; occupied requestStart/requestFinish decomposition through intrinsic residual endpoints; mapped complement-range helper; and newly the source/source signed residual target proof plus residualTarget canonical equality (last two not yet production-built).

Evidence: `context-canonical-sourcesource-production-01.log` Lake EXIT=0; `context-canonical-context-02.log` Scratch EXIT=0; `context-canonical-target-final-01.log` Scratch EXIT=0. Next: add canonical request aliases and residualContext disjointness under hC+hq, Lake-build target, report signatures. General target/target and opposite remain open.
